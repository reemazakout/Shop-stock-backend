{
  "name": "e-commerce-back",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port ${PORT}",
    "dev": "nodemon --watch db.json --exec 'npm start'"
  },
  "author": "Your Name",
  "license": "ISC",
  "description": "A simple JSON server for e-commerce backend",
  "dependencies": {
    "json-server": "^1.0.0-beta.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
